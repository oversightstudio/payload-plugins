{
  "name": "oversightstudio-plugins",
  "version": "1.0.0",
  "private": true,
  "description": "Payload plugins maintained by Oversight Studio.",
  "packageManager": "pnpm@11.13.0",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "cd dev && pnpm dev",
    "build": "pnpm --filter=./packages/* run build",
    "test": "pnpm --filter=./packages/* run test",
    "typecheck": "pnpm --filter=./packages/* run typecheck && pnpm --dir dev typecheck",
    "check": "pnpm typecheck && pnpm test && pnpm build",
    "verify:packages": "node scripts/verify-package.mjs",
    "audit:high": "pnpm audit --prod --audit-level=high",
    "release": "pnpm --filter=./packages/* publish --access public"
  },
  "keywords": [],
  "author": "oversightstudio",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "3.0.0",
    "prettier": "3.9.0"
  }
}
